# STM32F4_CAPTURE
STM32F401 demonstrating Timer9 Input Capture on PA3 and displaying the captured timer value on a 16x2 LCD using GPIO interface. The program reads the captured value from TIM9 CCR2 register and continuously updates it on the LCD.
